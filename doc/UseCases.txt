Use cases:

Shortening
1. App gets URL to shorten, user has selected perferred service previously. //Common, current model OK
2. App gets URL to shorten, user prompted to select service. //Less common, but maybe used if pref'd service down
3. App sends URL out to multiple services for vanity/other url purposes. //Least common. 

Server stuff